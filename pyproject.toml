[project]
name = "directory-organizer-local-llm"
version = "0.1.0"
description = "Safe local-LLM directory organizer CLI"
requires-python = ">=3.10"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
